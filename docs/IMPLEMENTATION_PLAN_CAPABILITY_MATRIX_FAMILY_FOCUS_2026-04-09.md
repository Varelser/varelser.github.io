# Capability Matrix Family Focus Plan — 2026-04-09

Goal: turn the Project I/O capability matrix from a read-only status view into a family-level action surface.

Scope:
- add row-level focus action for each family
- add row-level copy packet for each family
- keep actions inside Project I/O without needing global panel state
- preserve existing warning focus / implementation packet flows

Done when:
- each capability row exposes Focus family
- each capability row exposes Copy family
- copied packet includes current and manifest routes for the selected family
- typecheck and unit tests pass
