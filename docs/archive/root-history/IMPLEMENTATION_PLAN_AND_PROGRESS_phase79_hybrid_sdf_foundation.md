# Phase 79 — Hybrid SDF foundation

- Added `hybridSdfEnabled` runtime toggle.
- Added `SdfSurfaceShellSystem` as a second real execution foundation after hybrid volumetric.
- Routed `surface-shell` family into `hybrid-runtime` when the toggle is enabled.
- Added UI exposure for exercising the new foundation without editing config files.
