# Tailwind panel token consolidation

- Added shared panel font-size utility classes in `styles.css`: `text-panel`, `text-panel-sm`, `text-panel-xs`, `text-panel-lg`.
- Replaced `text-[10px]` / `text-[9px]` across `components/`.
- Moved `index.html` inline body/scrollbar styles into `styles.css`.
