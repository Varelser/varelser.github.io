# Optimization pass: project/export/compare lazy

- Global export section moved behind React.lazy in the global tab.
- Project manifest and compare sections moved behind React.lazy inside ControlPanelProjectIO.
- Verified with warning-free build, unit pass, and package-integrity pass.
